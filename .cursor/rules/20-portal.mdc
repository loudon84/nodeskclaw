---
description: Portal coding rules
globs:
  - "nodeskclaw-portal/src/**/*.vue"
  - "nodeskclaw-portal/src/**/*.ts"
alwaysApply: false
---

Portal 使用 Vue 3 + Vite + TypeScript + Tailwind + Pinia + vue-i18n。

规则：
- 新增用户可见文案必须加 i18n（vue-i18n），不允许硬编码中文。message_key 用小写点分层（如 errors.auth.token_invalid）。
- 图标使用 lucide-vue-next，不使用 emoji。禁止给 button 元素手动加 cursor-pointer（全局 CSS 已覆盖）。
- 滚动条样式走全局定义（globals.css），组件内禁止自定义 ::-webkit-scrollbar。
- 下拉选择使用自定义 button-based dropdown，禁止原生 select。
- API 调用走 src/services，页面组件避免直接写复杂请求逻辑。
- 跨页面共享状态走 Pinia stores，页面局部状态留在组件内。
- 修改路由时检查 router、layout、导航入口和权限。
- 修改逻辑后搜索同源副本（如 admin portal 中对应页面）同步修改。
- 默认先读取 .cursor/context/portal-codemap.md。
- 不要读取 backend、llm-proxy、ee、openclaw、vibecraft，除非任务明确要求。
