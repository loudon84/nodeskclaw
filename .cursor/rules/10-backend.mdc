---
description: Backend coding rules
globs:
  - "nodeskclaw-backend/**/*.py"
alwaysApply: false
---

后端分层：api（路由与权限） → schemas（请求/响应契约） → services（业务逻辑） → models（SQLAlchemy ORM） → alembic（迁移） → core（配置、依赖、权限、feature gate） → services/runtime（实例运行时、K8s、消息、上下文桥接）。

规则：
- 删除必须软删除（deleted_at），严禁物理删除。唯一约束使用 Partial Unique Index。
- 新增或修改 SQLAlchemy Model 字段，必须同步生成 Alembic 迁移（alembic revision --autogenerate），禁止手写 revision ID。
- API 错误返回必须包含 error_code + message_key + message。
- K8s 操作必须显式 --context，禁止依赖隐式当前集群。部署必须同时确认 context 和 namespace。
- Docker 构建/拉取必须指定 --platform linux/amd64。
- 变更 API 时同步检查 schemas、service、测试与前端调用。
- 解析 openclaw.json 等 JSONC 文件前必须剥离 // 行注释。
- NFS 操作时注意容器路径与本地挂载路径的转换。
- 修改 Gene 模板或 Agent 行为后需评估是否同步 Skill 定义。
- **双轨读档（需求分析）**：涉及 nodeskclaw-backend 功能变动时，先读 `docs/backend/index.md` 确认能力域与模块边界；再按任务域读 **1 份** 模块设计文档（见 `backend-codemap.md` 任务域映射表）。然后用 `.cursor/context/backend-codemap.md`（Hub）+ 1 个子索引定位改码路径：backend-api-index.md / backend-models-migration.md / backend-services-index.md；Runtime/K8s 任务读 runtime-codemap.md。设计文档定「做什么、契约与数据流」；CODEMAP 定「改哪些文件」——互补，不互相替代。
- **文档回写（实现 + review 通过后）**：若变更涉及新/改 API、数据模型、跨模块数据流、权限码、MCP 契约或环境变量，必须同步更新 `docs/backend/` 对应文档；跨域或新能力先更新 `index.md`，再更新模块专文。仅内部重构且无行为变化可不更新，须在 PR/回复中说明「无文档变更」。目录重组或新增路由文件时，酌情更新 CODEMAP 子索引（locator），不必把架构叙述写进 CODEMAP。
- 不要读取 portal、llm-proxy、ee、openclaw、vibecraft，除非任务明确要求。
